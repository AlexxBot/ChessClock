class Clock {
  String description;
  int duration; //este parametro considerar en minutos
  int increment;
  int movesToIncrement;

  Clock(
      {this.description, this.duration, this.increment, this.movesToIncrement});
}
